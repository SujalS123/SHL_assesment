# Core API framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# LLM + agent
langchain==0.2.6
langchain-community==0.2.6
langchain-groq==0.1.6

# Embeddings (runs locally, no API key needed)
fastembed

# Vector store (local file, no external service)
faiss-cpu

# Data validation
pydantic==2.7.4

# Scraping (for scripts/scrape.py — not needed at runtime)
requests==2.32.3
beautifulsoup4==4.12.3

# Testing
pytest==8.2.2

# Environment management
python-dotenv==1.0.1
